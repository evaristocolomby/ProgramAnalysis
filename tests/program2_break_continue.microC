/* Break and continue are good for Reaching Definitions. Break will get out of the loop and them we will know 
which label we are going to. The continue will be back for the While condition */

{ int counter;
  int z;
  counter = 0;

 while(1 < 2) 
 {
   counter = counter + 1;
   if(counter == 5) {continue;}
   if(counter == 9) {break;}
 }
 z = 1;
}
